# Canvas React App

This project provides a very small React-based canvas inspired by the Obsidian Canvas feature. You can add draggable notes on a dark themed background. To try it out, open `index.html` in a browser.

No build step is required as React and ReactDOM are loaded from a CDN.
